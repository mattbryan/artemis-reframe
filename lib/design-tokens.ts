/**
 * Design tokens for Brand & Philosophy and consistent UI.
 * Use these values for color decisions; Tailwind theme extends with brand.*.
 */

export const brandTokens = {
  border: "var(--border)",
  accent: "#4e8eff",
  primary100: "#eff6ff",
} as const;

export const textSecondary = "var(--muted-foreground)";
