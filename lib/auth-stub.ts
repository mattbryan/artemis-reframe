/**
 * Auth stub — placeholder for future authentication.
 * Replace with real auth provider when ready.
 */

export const isAuthenticated = (): boolean => {
  return true; // Stub: always pass during scaffold phase
};
